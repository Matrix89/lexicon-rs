mod lex;

fn main() {
    //let r = lex::Lexicon::App::Bsky::Feed::GetTimeline::main(10, "".to_owned(), "".to_owned());
    //for post in r.unwrap().feed {
    //println!("{}", post.post.uri);
    //}
}
